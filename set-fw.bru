meta {
  name: set-fw
  type: http
  seq: 3
}

post {
  url: http://{{ROUTER_IP}}/api/v1/firewall
  body: formUrlEncoded
  auth: inherit
}

headers {
  X-CSRF-TOKEN: {{CSRF_TOKEN}}
}

body:form-urlencoded {
  FirewallLevel: on
  FirewallLevelV6: on
}

script:pre-request {
  const requestResponse = await bru.runRequest("refresh");
}

settings {
  encodeUrl: false
}
